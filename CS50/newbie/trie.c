#include <stdio.h>
#include <stdlib.h>

typedef struct _trie
{
    char university[20];
    struct _trie* paths[10];
}
trie;

// A function to insert elements into trie
void insert(trie *root)
{

}

// A function to search for elements in trie
void search(int key,trie *root
void main(void)
{
    trie *start = malloc(sizeof(trie));
}

